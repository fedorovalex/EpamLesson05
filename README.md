- Ќаписать перечисление "ћес€цы года". ƒолжны быть методы получени€ количество дней, следующий мес€ц. 

- Ќаписать программу, получает на вход два операнда (целые числа), операцию (+, -, /, *) и выводит результат. ƒанные передаютс€ через командную: левый операнд (параметр -l), правый операнд (параметр -r), и операцию (параметр -o).